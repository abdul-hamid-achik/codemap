{
  "id": "03_callees",
  "category": "understand",
  "grader": "set_equal",
  "answer_key": "callees",
  "truth": "truth/03_callees.json",
  "prompt": "In this repository, list every function that the function `PlainClone` in package `git` calls directly (its immediate callees), INCLUDING standard-library functions. Use fully-qualified names: `package.Function` for package-level functions and `package.ReceiverType.Method` for methods (receiver type WITHOUT any pointer `*`). `package` is the Go package NAME (the root go-git package is `git`). Test-suite methods count too and are named like `git.RepositorySuite.TestFoo`. Answer ONLY as a json fenced block: {\"callees\": [\"pkg.Func\", ...]}."
}
